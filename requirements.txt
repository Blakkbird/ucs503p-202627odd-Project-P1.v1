# Runtime is deliberately standard-library only, for ingestion and
# for the model. The daily job runs unattended and nothing in its
# path should be able to break on a dependency upgrade -- which is
# also why code/model.py writes out a ridge fit by hand instead of
# importing scikit-learn. See docs/method.md.
#
# Everything below is for testing and linting only.

pytest>=8.0
ruff>=0.6
